{
  "scripts": {
    "swagger-cli": "swagger-cli",
    "validate": "swagger-cli validate src/index.yaml",
    "bundle": "swagger-cli bundle src/index.yaml --outfile"
  },
  "devDependencies": {
    "@apidevtools/swagger-cli": "^4.0.4"
  }
}
